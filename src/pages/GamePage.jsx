// 자리만 미리 만들어둔 화면 - 지금은 만장일치가 아닐 때 무작위 추첨이 resolve() 호출
// 한 번 안에서 조용히 끝난다(백엔드 gameparticipant.model.service.Game 참고). 나중에
// 이 추첨 과정을 참여자들이 직접 보는 미니게임 화면으로 만들 때 이 파일을 채우고,
// App.jsx에 라우트를 추가한다. 아직 라우팅에는 연결되어 있지 않다.
function GamePage() {
  return null
}

export default GamePage
