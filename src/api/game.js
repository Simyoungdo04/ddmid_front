// 자리만 미리 만들어둔 파일 - 지금은 만장일치가 아닐 때 무작위 추첨이 resolve() 안에서
// 조용히 일어난다(백엔드 gameparticipant.model.service.Game). 나중에 이 추첨을 실제
// 미니게임 화면(GamePage)으로 바꿀 때, 그 화면이 부를 API를 여기에 추가한다 -
// room.js/restaurant.js처럼 fetchXxx/createXxx/updateXxx/deleteXxx로 이름 붙인
// 개별 함수를 export한다(객체로 묶지 않는다).
